# App 逻辑主干流程图

> 本图先不展开疾病、情景、评分、提示等产品特性，只梳理 App 从进入到完成一次训练/测试，再到结果沉淀的核心逻辑。

```mermaid
flowchart TD
  A["启动 App"] --> B["初始化<br/>读取用户状态 / 基础配置 / 本地或远端数据"]
  B --> C["首页 Dashboard"]

  C --> D["训练入口"]
  C --> E["测试入口"]
  C --> F["路径入口"]
  C --> G["我的入口"]

  D --> H["任务列表页<br/>pages/scenes"]
  E --> I["测评列表页<br/>pages/tests"]
  F --> J["路径内容页<br/>pages/path"]
  G --> K["个人中心页<br/>pages/profile"]

  H --> L["选择任务"]
  I --> L

  L --> M{"选择交互方式"}
  M -->|"文字"| N["文字会话页<br/>pages/chat"]
  M -->|"语音"| O["语音会话页<br/>pages/voice"]

  N --> P["会话运行中"]
  O --> P

  P --> P1["加载任务上下文"]
  P1 --> P2["用户输入"]
  P2 --> P3["AI 生成回复"]
  P3 --> P4["记录对话过程"]
  P4 --> P5{"任务是否结束"}
  P5 -->|"否"| P2
  P5 -->|"是"| Q["生成结果"]

  Q --> R["报告页<br/>pages/report"]
  R --> S["保存记录"]
  S --> T["更新个人数据"]
  T --> K

  K --> U["查看历史记录 / 能力趋势 / 下一步建议"]
  U --> L

  J --> L
```

## 逻辑分层

```mermaid
flowchart TD
  A["页面层"] --> A1["首页"]
  A --> A2["任务列表"]
  A --> A3["会话页"]
  A --> A4["报告页"]
  A --> A5["个人中心"]
  A --> A6["路径内容页"]

  B["业务层"] --> B1["任务选择"]
  B --> B2["会话控制"]
  B --> B3["结果生成"]
  B --> B4["记录沉淀"]
  B --> B5["个人数据汇总"]

  C["数据层"] --> C1["用户信息"]
  C --> C2["任务配置"]
  C --> C3["会话记录"]
  C --> C4["结果报告"]
  C --> C5["成长数据"]

  D["AI 能力层"] --> D1["角色回复"]
  D --> D2["过程辅助"]
  D --> D3["结果评价"]
  D --> D4["建议生成"]

  E["约束层"] --> E1["身份与权限"]
  E --> E2["模式规则"]
  E --> E3["合规边界"]
  E --> E4["内容安全"]

  A3 --> B2
  B1 --> C2
  B2 --> D1
  B2 --> C3
  B3 --> D3
  B3 --> C4
  B4 --> C3
  B4 --> C4
  B5 --> C5
  E --> B2
  E --> D
```

## 主干说明

- 首页负责分发入口，不承载复杂业务。
- 训练和测试在任务选择后进入同一套会话主流程，只是模式规则不同。
- 文字会话和语音会话是交互方式差异，底层都进入“会话运行中”。
- 报告页是一次任务的收口，生成结果后再保存记录。
- 个人中心只消费沉淀后的记录和统计，不直接参与会话。
- 路径内容页是内容入口，可引导用户进入对应任务，但不应成为主流程的必经节点。
- App 的核心闭环是：选择任务 → 完成会话 → 生成报告 → 保存记录 → 更新个人数据 → 推荐下一次任务。
